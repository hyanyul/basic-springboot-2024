SELECT SEQ_TODOS.CURRVAL FROM DUAL;

SELECT * FROM USER_SEQUENCES WHERE SEQUENCE_NAME = 'SEQ_TODOS'

INSERT INTO PKNUSB.TODOS
(
	TNO
	, TITLE 
	, DUEDATE 
	, WRITER 
	, ISDONE 
)
VALUES 
(
	SEQ_TODOS.NEXTVAL
	, '피자 먹기'
	, TO_DATE('20240613 21:00:00', 'YYYY-MM-DD HH24:MI:SS')
	, '홍길동'
	, 0
);

SELECT TNO
	, TITLE
	, DUEDATE
	, WRITER
	, ISDONE
FROM TODOS;

SELECT * FROM TODOS WHERE TNO = 1;

UPDATE TODOS SET 
	TITLE = '피자 두 조각 먹기'
	, ISDONE = 1
WHERE TNO = 1;

DELETE FROM TODOS WHERE TNO = 1;